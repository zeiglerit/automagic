apiVersion: v1
kind: Service
metadata: 
  name: {{ include "nginx-webserver.fullname" . }}
spec: 
  type: {{ .Values.service.type }}
  ports: 
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector: 
    app: nginx-webserver 
