apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-apps
  namespace: argocd
spec:
  project: default

  source:
    repoURL: file:///mnt/c/Users/Me/Documents/git/mind/automagic/argo
    targetRevision: HEAD
      #https://github.com/YOUR_ORG/YOUR_REPO
      #targetRevision: main
    path: gitops/apps

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
