apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-sim.fullname" . }}
  labels:
    {{- include "infra-sim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-sim.selectorLabels" . | nindent 4 }}
