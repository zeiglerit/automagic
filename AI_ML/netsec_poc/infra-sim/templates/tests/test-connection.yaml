apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infra-sim.fullname" . }}-test-connection"
  labels:
    {{- include "infra-sim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infra-sim.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
